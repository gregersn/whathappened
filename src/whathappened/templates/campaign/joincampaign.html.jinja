{% extends 'base.html.jinja' %}

{% block header %}
  <h2>{% block title %}{{ campaign.title }}{% endblock %}</h2>
{% endblock %}

{% block content %}
    <p>
    You have been invited to join the campaign: <strong>{{ campaign.title }}</strong>
    </p>
    <p>
        <form action="" method="post">
            {{ joinform.hidden_tag() }}
            <p>{{ joinform.submit() }}</p>
        </form>
    </p>
{% endblock %}
